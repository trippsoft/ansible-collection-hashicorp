---
name: Generate Role Docs
'on':
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - galaxy.yml
      - roles/repo/.aar_doc.yml
      - roles/repo/README.md
      - roles/repo/meta/argument_specs.yml
      - roles/repo/meta/main.yml
defaults:
  run:
    working-directory: 'trippsc2.hashicorp'
jobs:
  docs_repo:
    name: Generate repo role documentation
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.hashicorp
          ref: main
      - name: Generate repo role documentation
        working-directory: 'trippsc2.hashicorp'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/repo
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update repo role documentation." || /bin/true
          git push origin main
          deactivate
